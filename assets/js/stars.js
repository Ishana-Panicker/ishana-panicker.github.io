const canvas=document.getElementById('starfield');const ctx=canvas.getContext('2d');let stars=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight;stars=Array.from({length:Math.min(260,Math.floor(innerWidth*innerHeight/5200))},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*1.6+.25,s:Math.random()*.45+.08,a:Math.random()*Math.PI*2}))}resize();addEventListener('resize',resize);function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const st of stars){st.a+=st.s*.03;const glow=(Math.sin(st.a)+1)/2;ctx.beginPath();ctx.arc(st.x,st.y,st.r,0,Math.PI*2);ctx.fillStyle=`rgba(${170+glow*85},${190+glow*50},255,${.35+glow*.65})`;ctx.fill();st.y+=st.s*.08;if(st.y>canvas.height)st.y=0}requestAnimationFrame(draw)}draw();function shooting(){const el=document.createElement('div');el.className='shooting-star';el.style.left=(55+Math.random()*45)+'vw';el.style.top=(Math.random()*45)+'vh';document.body.appendChild(el);setTimeout(()=>el.remove(),1600)}setInterval(shooting,2400);
